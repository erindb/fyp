// index.js
module.exports = {
  regex: {
    match: function(regexpStr, string) {
      var re = new RegExp(regexpStr);
      return string.match(re);
    }
  },
  range: function(a, b) {
  	var lst = [];
  	for (var i=a; i<b; i++) {
  		lst.push(i)
  	}
  	return lst;
  }
};
